# varialink
```
sudo apt update -y && sudo apt full-upgrade -y && sudo apt dist-upgrade -y && sudo apt install git python3 python3-pip python-tk python3-tk -y && git clone https://github.com/OishiiCha/varialink.git && cd varialink && python3 desktop_file.py

```
